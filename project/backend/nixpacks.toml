[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -m app.seed || echo 'Seed already done'"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
